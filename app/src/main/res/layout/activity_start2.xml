<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_start2"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="sr.unasat.financialapp.activities.start.StartActivity">

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:srcCompat="@drawable/next"
        android:layout_alignParentBottom="true"
        android:layout_toRightOf="@+id/opening_balance"
        android:layout_toEndOf="@+id/opening_balance"
        android:layout_marginBottom="24dp"
        android:id="@+id/imageView"
        android:onClick="startActivityEvent"/>

    <ImageView
        app:srcCompat="@drawable/logo"
        android:id="@+id/imageView3"
        android:layout_width="250dp"
        android:layout_height="250dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true" />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="textPersonName"
        android:ems="10"
        android:id="@+id/emailfield"
        android:hint="your email ..."
        android:textAlignment="center"
        android:layout_below="@+id/imageView3"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="55dp" />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="numberDecimal"
        android:ems="10"
        android:id="@+id/opening_balance"
        android:hint="your opening balance ..."
        android:textAlignment="center"
        android:layout_marginBottom="30dp"
        android:layout_above="@+id/imageView"
        android:layout_alignLeft="@+id/emailfield"
        android:layout_alignStart="@+id/emailfield" />

</RelativeLayout>
